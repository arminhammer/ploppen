/**
 * Created by armin on 9/28/14.
 */
"use strict";

describe("FileTransferServer service", function () {

    //var ioc = require('socket.io-client');

    beforeEach(module('ploppen'));
    beforeEach(module('ploppen.filetransfer'));

    /*
    it('Should return an empty peer list by default', inject(function (FileTransferServer) {

        FileTransferServer.start();
        expect(FileTransferServer.updateFileList()).toEqual(true);

    }));
    */

});
