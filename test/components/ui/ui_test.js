'use strict';

describe('ploppen.ui module', function() {

  beforeEach(module('ploppen.ui'));

  describe('ui controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      //var uiCtrl = $controller('UIController');
      //expect(uiCtrl).toBeDefined();
    }));

  });
});